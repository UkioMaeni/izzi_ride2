import 'package:flutter_stripe/flutter_stripe.dart' as stripe;

class PaymentService {
  
  initPay() async {
    
    //final response = await stripe. fetchPaymentIntentClientSecret();
    stripe.CardField();
  }

}